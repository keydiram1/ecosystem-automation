[local]
localhost ansible_python_interpreter=python3 ansible_connection=local
[nodes]
%{ for index, ip in nodes ~}
${prefix}-node-${index} ansible_host=${ip} ansible_user=ec2-user ansible_ssh_private_key_file=${ssh_private_key}
%{ endfor ~}
